[package]
name = "rockup"
version = "0.1.0"
edition = "2021"

[dependencies]
clap.workspace = true
toml.workspace = true
rock-shared = { path = "../rock-shared", features = ["manifest", "sysroot", "process"] }
