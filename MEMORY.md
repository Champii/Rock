# MEMORY.md

## Workflow
- Always use lean-ctx tools for repository reads, searches, directory exploration, and shell commands; include this requirement explicitly in every subagent prompt so delegated work also uses lean-ctx.
- Use at most two subagents at the same time because of rate limits; this repo benefits from implementation subagents followed by separate spec-compliance and code-quality reviews.
- Do not run multiple test suites in parallel. For longer runs, save output once under `/tmp` and inspect the log instead of rerunning the full suite repeatedly.
- For `cargo test ... -- --exact`, use fully qualified test paths when short names are ambiguous or can run zero tests.
- Recent OpenCode subagent prompts for this repo repeatedly prohibit commits, staging, pushes, VCS mutations, and `.sisyphus/` access. Follow the current prompt over the beads session-completion block and do not touch `.sisyphus/` unless explicitly asked.
- When reporting TDD-oriented plan tasks, preserve any required report format and list every RED/GREEN command that was actually run; recent reviews rejected incomplete command reporting.
- In review-only tasks, do not modify files and return the requested verdict/findings format exactly; recent corrections rejected drifting into implementation or incomplete review responses.
- For clean-slate audit reviews on this branch, obey the prompt's explicit boundaries: use lean-ctx tools, do not edit in review-only sessions, do not mutate VCS, and do not inspect `.sisyphus/` unless the prompt explicitly allows it.
- When reviewing phased implementation plans, judge compliance against the current task boundary, not the final end-state of later tasks. Recent Task 8 reviews required reassessing findings because builtin indexing residue was explicitly allowed through earlier tasks and deleted only in later tasks.
- For closure ledgers or residue audits, base counts on saved command output and exact categories rather than hand counts. Recent corrections required separating production, `cfg(test)`, fixtures, and historical strings, and avoiding brittle line-number claims.
- For the HIR codegen debt-removal branch, treat `/root/new_lang2/.worktrees/hir-codegen-debt-removal` as the source of truth. Do not use CodeGraph for this branch unless the current prompt explicitly permits it and the index is known to point at this worktree.
- Before patching this branch, re-read the target file section and verify whether a referenced module is a file or directory; recent failures came from stale patch context and guessed paths such as `crate_artifact.rs` vs `crate_artifact/`.
- When comparing or merging worktrees, check `git status --short` and confirm new source modules are tracked before trusting a passing test run. The `stdlib-file-io-v2` worktree passed locally while `stdlib/fs.rk` was untracked, so the merged `develop` checkout failed until that module was added.
- Recent user requests prefer reusable stdlib support over example-level workarounds: move networking/I/O errors, `Read`/`Write`, `Show` impls, env args, and string/vector conveniences into `stdlib/` instead of patching `test_projects/new_new/main.rk` with local enums or raw libc comparisons.
- For `test_projects/new_new/main.rk`, the user wants a showcase of Rock expressiveness and brevity. Prefer functional composition, direct operator chaining, auto-deref, and stdlib abstractions over imperative temporary variables, `server_*` helper wrappers, `Arc::get!`, local display wrappers, or unnecessary parentheses.
- For README and beginner-book docs, prefer RustBook-like explicit teaching: every explained feature gets code, Rock fences should be self-contained unless a fragment convention is explicit, and user-facing workflows should use the `rock` binary rather than asking users to invoke `rockc`.
- README examples should showcase expressive Rock syntax with evolved, practical programs such as `test_projects/new_new/main.rk`; avoid purely descriptive snippets, stale roadmaps, and stale `new_lang` documentation links for this `Champii/Rock` repo.
- When working on `rock run` dev sysroot or root-executable spawning, the user expects the `rock` binary to find and spawn the adjacent `rockc` binary from the build output, not require `rockup` or derive paths from the current Rock project directory such as `/.l2s/rockc`. Reproduce from `test_projects/new_new` with `cargo run -p rock -- run -- listen` when this area changes.
- `CLEAN_SLATE_COMPILER_AUDIT.md` is the detailed whole-codebase audit for this branch. Check it before writing specs or implementation plans for the remaining cleanup.
- Task 5 was closed on 2026-07-14. `docs/superpowers/specs/2026-07-11-task-5-authoritative-closure-ledger.md` is the source of truth and supersedes earlier narrower findings and prior completion claims; it records closed findings F1-F64 and the final gate evidence. Add any future Task 5 regression to that ledger before fixing it.
- When the user asks whether an audit step is fully done, separate the scoped step verdict from the broader clean-slate/string-identity audit. Step 2 only covered production codegen name-keyed layout side tables; broader string/name-based identity remains in later audit items.

## Compiler Architecture
- Current compiler cleanup prefers canonical IDs for semantic decisions. `DefId`, `InstanceId`, `HirVarTarget`, and `HirCallTarget` should drive callable selection when available; strings should remain compatibility, display, diagnostic, artifact metadata, or local lexical metadata.
- `generic_function_by_id` and `external_generic_functions_by_id` are the preferred resolved generic lookup paths. `*_by_compat_name` helpers are for unresolved or legacy compatibility inputs only.
- When updating `docs/superpowers/plans/master-audit-checklist.md` or roadmap docs, avoid overclaiming. String-keyed mono semantic lookup is complete only where canonical IDs are available; legacy non-pipeline HIR/name pruning helpers are removed; instance body representation and MIR/codegen metadata cleanup remain follow-ups.
- For future cleanup/removal tasks, do not add permanent source-content audit tests that only assert deleted symbol names or old files are absent. Use manual `rg`/source validation for residue checks unless the user explicitly asks for a durable test or the test verifies real semantic behavior.
- This project is in a prototyping phase: do not preserve deprecated code paths, backward-compatibility shims, or transitional aliases unless explicitly requested.
- MIR borrow checking is converging on indexed place/move/loan IDs. Keep `PlacePathId`, `MovePathId`, `LoanId`, and `LoanTable` as the semantic identity model; retain structural `Place` values mainly for diagnostics and compatibility inputs.
- Do not reintroduce the legacy `BorrowId`, `LiveBorrowSet`, or `compute_live_borrows` island. `BorrowData` is an ID-less temporary collection record, while active borrow checking uses `LoanState`, `ReferenceLiveness`, and statement-precise owner release.
- For path-aware initialization/move checks, `InitMap::join` must make `Moved` take precedence over `Init` so may-move behavior survives CFG joins. Move-path checks should reject exact moved paths, moved ancestors, uninitialized ancestors/exact paths, and parent/root use when a descendant was moved.
- Owned heap soundness is enforced in MIR. Borrow checking rejects moving or copying non-`Copy` values out of shared references, while raw-pointer dereference moves remain allowed for unsafe containers.
- `match *ref_expr` on a reference scrutinee is lowered with `MatchScrutineeMode::Borrowed`; enum payload bindings become shared references and must not mark the original scrutinee as moved. Plain by-value `match value` remains the consuming path.
- Option/Result consuming stdlib combinators such as `map`, `and_then`, `or`, `flatten`, `fold`, `unwrap_or`, `>>=`, `<|>`, and `<&>` use move receivers (`~@`). `Show`/`println` stay shared and rely on borrowed payload bindings; `Option::inspect` is currently consuming and returns the original option.
- MIR reference codegen must not promote temporary references to heap owners. Keep `compile_mir_ref_rvalue` returning the MIR place address for thin references and preserve tests that assert generated IR does not contain `mir_ref_tmp_alloc`.
- Trait-bound method calls must use `receiver_adjustment_candidates` and preserve the selected `ReceiverAdjustment`; do not add selection paths that bypass the concrete receiver-candidate machinery.
- The HIR codegen debt-removal target pipeline is parser/macro/lower/resolve owning source names, MIR builder producing MIR plus one authoritative backend contract, and codegen consuming only MIR plus that contract. Strings should remain display/diagnostic/artifact text, not semantic lookup, layout, ABI, drop-glue, projection, callable, or backend-dispatch keys.
- `MirBackendMetadata` removal is complete in Rust source for this branch. Do not reintroduce metadata sidecars, `from_legacy_metadata*` adapters, `sync_test_backend_contract`, or default/repair contract fixtures; future backend work should preserve `MirBackendContract` as the only MIR backend authority and use realistic contract fixtures.
- Step 1's durable invariant is direct, complete `MirBackendContract` construction: do not reintroduce staging rows, struct/enum display-alias traversal, or a partial agreement mode. Runtime requirements must be derived by the shared MIR observer and match exactly in agreement/codegen preflight; codegen must not unconditionally declare runtime helpers. Builder and codegen tests must use complete contract-native fixtures rather than repair/default fixtures.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 3 is complete: backend symbols now live only in `ProductLinkData.records` and loaded `ExternCrateLink.backend_symbols`; do not reintroduce `ProductIdentityTable.backend_symbols` or fallbacks from `qualified_name`, display names, interface names, impl type names, trait names, or method names for object-backed dependency symbols.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 4 split HIR `qualified_name` into explicit concepts. Do not reintroduce `HirFunction.qualified_name`, product/artifact function-row `qualified_name`, backend-shaped impl fallbacks, `impl_method_backend_symbol`, or `trait_default_backend_symbol`; mono backend symbols should come from `InstanceOrigin`/`DefId` plus substitution IDs, with `main` preserved as `main`.
- Canonical/display names for artifacts belong in `ArtifactCrateInterface.canonical_names`, product identity/display metadata, or resolver entries. Dependency static impl method validation should use the dependency interface canonical name; object-backed backend symbols must still come only from link records/backend symbol maps.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 5 is complete: selection authority is ID-based across HIR, products/artifacts, mono, DCE, and MIR. Do not reintroduce method dispatch by receiver/type display names, `MirCallable::Method`, `MirSelectedMethodMetadata`, targetless builtin-index guards, or unresolved method call edges in final MIR.
- Method calls whose receiver is still a `TypeVar` during lowering are selected in the post-inference accepted-HIR pass; keep that boundary typed and canonical instead of restoring mono/MIR rediscovery. `selection/legacy_lookup.rs` is deleted; do not recreate downstream type-name method matching, even as a compatibility bridge.
- Product artifact format version is `37` in both `rock-lib` and `rock-shared` after persisted typed method authority was added; update both constants together for future incompatible artifact schema changes.
- F65 static-method authority is closed. Keep canonical owner resolution strict: a recognized struct/enum owner with no matching static method must not fall through to raw variable lookup. Qualified static resolution must persist exact impl/method/trait/member authority; lowering must derive owner-to-method generic relations structurally by `GenericParamId`, compose owner and method substitutions into selected trait arguments, and never restore `static_method_target_for_callee` or display-name correlation.
- Static method validation for dependency artifacts should use dependency interface canonical names for authority checks while preserving object-backed backend symbols from link records/backend symbol maps only.
- In the Task 4 collection/source-index refactor, collection owns exact source-backed IDs. `LocalCollector` carries module identity and declaration order so current-source and artifact headers are built with canonical `ItemRecord.def_id`; do not restore post-hoc local declaration/impl ID repair, `resolve_named_hir_def_id`, or name-based repair fallbacks.
- Source-backed module body lowering should happen through one provider traversal. Root/source shells may recurse for structural IDs and diagnostics, but body lowering must avoid duplicate recursive traversal of the same source-backed module; inline bodies remain the recursive path.
- Explicit `lang` markers now define compiler-recognized language items; semantic roles must come from marker-bound canonical IDs, not trait/enum/member source names. Keep marker binding tied to `ItemRecord.def_id`, collected member IDs, `AssocTypeId`, and `VariantId`.
- `HirLanguageItems` is the shared `LanguageItems<DefId>` registry and should flow from collection through lower, inference, accepted HIR, mono, products, and artifacts without late discovery or setter mutation. Do not restore Drop-only language-item structs or post-inference provider rescans.
- Product artifact format version is `38` in both `rock-lib` and `rock-shared` after generic language-item registry serialization; update both constants together for future incompatible artifact schema changes.
- Language-item provider merging is deterministic and should report every conflicting protocol provider when using the all-conflicts path. Same-name self metadata should not be merged as an external dependency provider.
- Task 8 trait-owned index semantics should replace targetless builtin indexing with ordinary stdlib `Index` and `IndexMut` trait calls. Selection must use marker-bound trait/member/output IDs, assignment-place context must choose `IndexMut`, and MIR/codegen should see ordinary calls returning `&T` or `&mut T`, not structural index projections or `BuiltinIndex` sentinels.
- For Task 8, artifact format version should advance to `39` in both `rock-lib` and `rock-shared` when IndexMut is persisted; do not add a format-38 compatibility decode branch unless explicitly requested.
- Stdlib indexing behavior belongs in `stdlib/index.rk`, `stdlib/prelude.rk`, and `stdlib/vec.rk` using existing primitives such as `ArrayLen`, `ArrPtr`, and `PtrOffset`. Bounds checks should remain ordinary Rock code, and stdlib Index/IndexMut bodies must not recursively use `ptr[i]`.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 9 is complete: parser/AST syntax uses `ast::SelfReceiverMode`, collection/lowering convert it to `types::ReceiverMode`, and HIR/infer/selection/mono/MIR/products/artifacts use only the semantic type. Keep MIR ABI pass modes separate and do not restore AST receiver types downstream.
- Product artifact format version is `40` in both `rock-lib` and `rock-shared` after semantic receiver modes replaced AST receiver modes in serialized interface/body rows.
- Task 10 strict type finalization makes `ConstraintStore` plus `InferenceEngine::apply_numeric_defaults` the only type-defaulting authority. Accepted local programs and loaded artifacts must be free of `Type::TypeVar` and `Type::Error`; `Type::Generic` remains valid finalized declaration state, and product artifact format version is `41` in both `rock-lib` and `rock-shared`.
- For Task 10, do not restore `finalize_lenient`, lenient HIR walkers, `finalize_with_unresolved_default`, `finalize_strict_with_unspanned_default`, `ProductTypeRow::Error`, or mono/codegen recovery fallbacks for malformed HIR/artifact bodies.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 11 is complete: Rock has no exception or stack-unwind semantics, so `Terminator::Call.cleanup`, `Terminator::Drop.unwind`, and `MirAssertKind::NonNull` are deleted. `Constant::TypeId` is compile-time intrinsic metadata allowed only in complete type-only intrinsic argument lists validated by MIR agreement; `Constant::Callable` must use dedicated callable materialization paths.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 12 is complete: `compile_mir_program_unchecked_for_test` and unchecked whole-program MIR LLVM scaffolds are deleted. Successful MIR LLVM fixtures should go through `CodeGen::compile_program_from_mir` with explicit `MirProgramFixtureBuilder` or `compile_valid_mir_program` contracts; malformed fixtures should be named rejection tests that still use production validation and must not silently repair missing facts.
- `CLEAN_SLATE_COMPILER_AUDIT.md` Step 13 is complete: `semantic_identity_audit` protects production-source ownership boundaries for removed MIR backend sidecars, hardcoded `Sized` trait identity strings, HIR display-alias consumption in mono/MIR/codegen, type-name method rediscovery, string-to-item-ID recovery after resolution, and AST receiver syntax past lowering. Keep audits scoped to production ownership surfaces instead of broad vocabulary bans.
- The 2026-08-02 final closure completed Step 6 at the artifact boundary: `ArtifactCrossCrateHir` and `ExternBodyProviders` carry accepted generic functions and trait defaults by `DefId`; artifact interface/body rows must never be admitted or filtered by display-name availability.
- The final Step 10 MIR barrier rejects `TypeVar`, `Error`, and runtime `Generic` types. Generic nominal-layout templates are the only exception and must reference their layout's declared `GenericParamId`s. Do not restore LLVM integer placeholders, unresolved/error drop no-ops, `PtrOffset` `i8` element fallbacks, or mono-created `Type::Error` receiver authority.
- Final MIR callable operands can contain only `MirCallable::Resolved(MirCallableKey)`. Do not restore direct `Function`, `Extern`, `Instance`, `Closure`, `Intrinsic`, or `RuntimeHelper` variants or `UnresolvedCallableOperand` validation paths.
- The unimplemented public `rock test` subcommand was removed; keep it absent until a real implementation exists.
- The `generic-try-short-circuit` worktree is already contained in current `develop`; do not merge or reimplement it again unless new unique commits appear. Porting `stdlib-file-io` should be done from current `develop` using the old branch as a reference, not by blindly merging the pre-cleanup branch.
- The `stdlib-file-io-v2` port is intentionally stdlib/test-level work: it adds `IoError`, shared `Read`/`Write` traits, RAII `File`, and `TcpStream` trait impls without compiler or inference changes. Tests should prefer the existing `?` operator over nested `Result` matches, and TCP process tests should use the timeout-aware harness.
- Repeat-array literals such as `[0; 256]` must not expand into one MIR/codegen assignment per element. Keep the initializer evaluated once, require `Copy` initializers, and lower/codegen large fixed arrays with loop/fill behavior so examples such as `test_projects/new_new/main.rk` stay fast to compile.
- Rust-style callable/threading support is intentionally built on canonical language items and existing function-type metadata: `FnOnce`/`FnMut`/`Fn`, `Send`, and `Sync` live in `stdlib/callable.rk`, `stdlib/thread_safety.rk`, and `stdlib/thread.rk`; preserve artifact serialization for callable kind/capture types and do not add stdlib-name special cases.
- Thread spawning safety should reject borrowed captures, method values that can outlive borrowed receivers, and downstream user-defined fake `Send` impls; `JoinHandle` ownership/drop and pthread ABI details belong in stdlib/runtime-facing code, not in example programs.
- The higher-kinded-types / functional abstractions design is intended to be production-ready rather than a Rust-iterator-style shortcut. Preserve explicit binder syntax and ownership, rigid-spine inference, partial beta rules, decidable coherence grammar, supertrait-cycle checks, associated projection reduction, stable constructor fingerprints, runtime nominal constructor arguments, and exact mono -> MIR -> borrow-check ordering.
- HKT syntax should use `F _` for constructor holes, with comma/parenthesis grouping for multiple holes such as `Compose (F _), (G _), A`; do not require `F<_>` syntax or mandatory generic-parameter declaration lists unless the current spec changes.
- HKT implementation planning must include staged production caller migration, `CompilationIdentityContext` before mono, alias transport, all mechanical type walkers, an explicit coherence pipeline hook, staged artifact decode/remap APIs, artifact envelope/API/resource limits, associated-constructor parser/lowering, `TypeFacts` classification, reproducible `new_new` artifact/build/run gates, and updates for the current artifact format version.
- HKT functional operators are stdlib-owned, not compiler-owned. Keep `|>`, `>>=`, `<|>`, and `<&>` definitions in stdlib/prelude/type impls and cover them with integration tests such as `test_stdlib_option_result_fp_operators`; do not add parser, lowerer, or compiler fallbacks for their meanings.
- The fixed-point HKT constraint solver design requires one monotonic worklist per inference SCC: obligations with unknown heads remain `Pending`, never `Solved`, until rigid evidence is available, and dependent callable, constructor-equality, `Try`, and authority obligations must wake after type-variable generation changes.
- For annotation-free `test_projects/new_new/main.rk`, the durable goal is executable-local annotation removal only; nominal struct field/layout types remain required, and fixes should improve inference/stdlib abstractions rather than reintroducing local annotations or example-specific wrappers.
- Function-call holes use `_` to desugar calls into lambdas, but when the hole would only occupy trailing arguments, prefer currying instead of writing explicit trailing holes.
- Casts should bind tightly enough that `second (slice as I64)` keeps `second` bound to the cast result; do not require extra parentheses around `as` expressions or `&mut` expression forms unless the grammar truly needs them.
- Unit-return function and lambda syntax uses `!->`; declarations like `myfn !-> 0` should evaluate the body for effects and return `()`.
- Generic trait-bound method search should follow deref/deref-mut receiver adjustments, so stdlib wrapper types such as `Arc`/`Mutex` do not need ad hoc `.get!` methods or caller-side `&*value` workarounds.
- For Task 14 / `new_new` HKT closure work, compile a fresh stdlib artifact before direct `rockc` runs instead of assuming `build/stdlib.rkca` exists. Keep deterministic fresh-artifact regression tests self-contained instead of compiling mutable projects under `test_projects/`.
- For `Vec::map_ref` and `new_new` HKT refactors, inline closures over borrowed parameters need contextual callable typing; keep the `test_stdlib_vec_map_ref_contextually_types_closure_parameter` gate when replacing named adapter functions with inline closures.
- Treat `cargo clippy --workspace --all-targets -- -D warnings` as a separate cleanup task while the known `clippy::result_large_err` baseline remains; ordinary `cargo clippy --workspace --all-targets` can be used as the non-deny warning gate when the plan records that exception.

## Useful Commands
- Focused mono cleanup tests seen recently include `cargo test -p rock-lib mono::process::tests::process_resolved_generic_function_value_uses_def_id_not_reference_name -- --exact`, `cargo test -p rock-lib mono::process::tests::process_resolved_generic_call_uses_def_id_not_reference_name -- --exact`, and `cargo test -p rock-lib mono::process::tests::process_resolved_generic_function_argument_uses_def_id_not_reference_name -- --exact`.
- After focused tests pass, use `cargo fmt --all --check`, `git diff --check`, and then `cargo test -p rock-lib` when broader verification is warranted.
- Recent focused borrowck/dataflow verification used `cargo test -p rock-lib mir::borrowck -- --nocapture`, `cargo test -p rock-lib mir::dataflow -- --nocapture`, `cargo test -p rock-lib mir::dataflow::analyses::init -- --nocapture`, and `cargo test -p rock-lib mir::borrowck::paths -- --nocapture`.
- Recent owned-heap verification repeatedly used `cargo test -p rock-lib --test integration test_borrowed_enum_match_payload_binding_is_shared_reference -- --exact`, `cargo test -p rock-lib --test integration test_move_non_copy_value_out_of_shared_reference_is_rejected -- --exact`, `cargo test -p rock-lib --test integration test_stdlib_option_methods -- --exact`, `cargo test -p rock-lib --test integration test_stdlib_result_methods -- --exact`, `cargo test -p rock-lib --test integration test_stdlib_option_map_owned_string_consumes_receiver -- --exact`, and `cargo test -p rock-lib --test integration test_stdlib_result_bind_owned_string_forwards_move_receiver -- --exact`.
- Receiver-adjustment verification includes `cargo test -p rock-lib selection::service -- --nocapture`, `cargo test -p rock-lib selection::service::tests::selection_service_bound_mut_receiver_uses_autoref_mut_candidate -- --exact --nocapture`, `cargo test -p rock-lib selection::service::tests::selection_service_bound_shared_receiver_on_ref_uses_deref_candidate -- --exact --nocapture`, `cargo test -p rock-lib --test integration trait_bound_mut_receiver_call_autorefs_mut_reference -- --exact --nocapture`, and `cargo test -p rock-lib --test integration trait_bound_shared_receiver_on_ref_does_not_double_borrow -- --exact --nocapture`.
- HIR codegen debt-removal verification recently used the zero-residue search for legacy backend metadata strings under `lib/src`, `cargo test -p rock-lib codegen -- --nocapture`, `cargo test -p rock-lib codegen::mir_llvm -- --nocapture`, `cargo test -p rock-lib mir::agreement -- --nocapture`, `cargo test -p rock-lib mir::backend_contract -- --nocapture`, `cargo test -p rock-lib mir::builder -- --nocapture`, `cargo test -p rock-lib crate_artifact -- --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo test -p rock-lib --test integration`, `cargo fmt --all --check`, and `git diff --check`.
- Task 10 strict type finalization verification used `cargo test -p rock-lib hir::type_ids -- --nocapture`, `cargo test -p rock-lib accepted_hir -- --nocapture`, `cargo test -p rock-lib products -- --nocapture`, `cargo test -p rock-lib crate_artifact -- --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo test -p rock-lib --test integration`, a saved full `cargo test -p rock-lib` log under `/tmp/opencode/task10-rock-lib.log`, a saved Clippy log under `/tmp/opencode/task10-clippy.log`, `cargo fmt --all --check`, and `git diff --check`.
- Task 11 MIR codegen completeness verification used `cargo test -p rock-lib mir::agreement -- --nocapture`, `cargo test -p rock-lib mir::builder -- --nocapture`, `cargo test -p rock-lib mir::dataflow -- --nocapture`, `cargo test -p rock-lib mir::borrowck -- --nocapture`, `cargo test -p rock-lib codegen -- --nocapture`, `cargo test -p rock-lib --test integration`, a saved full `cargo test -p rock-lib` log, `cargo clippy --workspace --all-targets`, `cargo fmt --all --check`, `git diff --check`, and residue checks for removed cleanup/unwind/assertion/codegen fallback paths.
- Task 12 contract-fixture verification used `cargo test -p rock-lib mir::agreement -- --nocapture`, `cargo test -p rock-lib codegen::mir_llvm -- --nocapture`, `cargo test -p rock-lib codegen -- --nocapture`, `cargo test -p rock-lib --test integration`, a saved full `cargo test -p rock-lib` log, `cargo clippy --workspace --all-targets`, `cargo fmt --all --check`, `git diff --check`, and residue scans for unchecked MIR compile helpers and repair/default contract fixtures.
- Task 13 clean-slate regression audit verification used `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, a saved full `cargo test -p rock-lib` log, `cargo clippy --workspace --all-targets`, `cargo fmt --all --check`, and `git diff --check`.
- Step 2 layout cleanup verification used `cargo test -p rock-lib codegen::types -- --nocapture`, `cargo test -p rock-lib codegen -- --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo fmt --all --check`, `git diff --check`, and the `lib/src/codegen` residue scan for `struct_info|enum_info|struct_generic_owners|enum_generic_owners|struct_generic_param_ids\b|enum_generic_param_ids\b|struct_substitution\(`.
- Step 3 link-record cleanup verification used `cargo test -p rock-lib products -- --nocapture`, `cargo test -p rock-lib crate_artifact -- --nocapture`, `cargo test -p rock-lib crate_system -- --nocapture`, `cargo test -p rock-lib mono::external -- --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo test -p rock-lib --test integration`, `cargo fmt --all --check`, and `git diff --check`.
- Step 4 qualified-name cleanup verification used `cargo check -p rock-lib`, `cargo test -p rock-lib hir -- --nocapture`, `cargo test -p rock-lib collect::headers -- --nocapture`, `cargo test -p rock-lib lower::collect::traits -- --nocapture`, `cargo test -p rock-lib mono -- --nocapture`, `cargo test -p rock-lib products -- --nocapture`, `cargo test -p rock-lib crate_artifact -- --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo fmt --all --check`, and `git diff --check`.
- Step 5 ID-based method authority verification used `cargo test -p rock-lib`, `cargo test -p rock-lib crate_artifact::tests::test_compile_with_stdlib_artifact -- --exact --nocapture`, `cargo test -p rock-lib crate_artifact::tests::test_stdlib_product_artifact_links_static_impl_method -- --exact --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo fmt --all --check`, and `git diff --check`.
- F65 static-method authority verification recently used `cargo test -p rock-lib lower::paths -- --nocapture`, `cargo test -p rock-lib semantic_identity_audit -- --nocapture`, `cargo test -p rock-lib crate_artifact -- --nocapture`, `cargo test -p rock-lib --test integration test_static_method_value_preserves_selected_authority -- --exact --nocapture`, `cargo check -p rock-lib --tests`, `cargo fmt --all --check`, and `git diff --check`.
- Language-item task verification recently used `cargo test -p rock-lib parser::items::tests::items -- --nocapture`, `cargo test -p rock-lib fmt:: -- --nocapture`, `cargo test -p rock-lib macro_expansion:: -- --nocapture`, `tree-sitter generate && tree-sitter test`, `cargo test -p rock-lib collect:: -- --nocapture`, `cargo test -p rock-lib products -- --nocapture`, `cargo test -p rock-lib crate_artifact -- --nocapture`, `cargo test -p rock-lib merge_language_items_ -- --nocapture`, `cargo test -p rock-lib lowering_pipeline_preserves -- --nocapture`, `cargo check -p rock-lib --tests`, `cargo fmt --all --check`, and `git diff --check`.
- Run `tree-sitter generate` and `tree-sitter test` from `tree-sitter-rock/`, not from the repository root where `grammar.js` does not exist.
- Task 8 verification should stay serialized. Save the full `cargo test -p rock-lib` suite once under `/tmp/opencode/task8-rock-lib.log` when the plan asks for it, and use `tree-sitter test` from `tree-sitter-rock/` for language-item corpus changes.
- Task 8 closure verification recently centered on `cargo test -p rock-lib --test integration marked_slice_index_mut_updates_fixed_array -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_slice_index_assignment -- --exact --nocapture`, `cargo test -p rock-lib --test integration index_syntax_selects_separate_read_and_write_authority -- --exact --nocapture`, `cargo test -p rock-lib --test integration shared_index_borrow_blocks_index_mut_assignment -- --exact --nocapture`, `cargo test -p rock-lib selection::service -- --nocapture`, `cargo test -p rock-lib lower::control_flow::secondary -- --nocapture`, `cargo test -p rock-lib`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all --check`, and `git diff --check`.
- Repeat-array verification recently used `cargo test -p rock-lib parser::items::tests::literal::test_parse_array_repeat -- --nocapture`, `cargo test -p rock-lib fmt::tests::format_repeat_array_literal -- --exact --nocapture`, `cargo test -p rock-lib codegen::mir_llvm::tests::mir_codegen_lowers_repeated_array_as_fill_loop -- --exact`, `cargo test -p rock-lib --test integration test_repeat_array_initializer_is_evaluated_once -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_repeat_array_rejects_non_copy_initializer -- --exact --nocapture`, and `cargo test -p rock-lib --test integration test_stdlib_fixed_u8_arrays_convert_to_string_for_any_length -- --exact --nocapture`.
- Threading/callable verification recently used `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_join_returns_value -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_accepts_owned_send_capture -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_rejects_borrowed_capture -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_rejects_borrowed_capture_after_branch_merge -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_rejects_borrowed_method_value -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_rejects_downstream_send_impl -- --exact --nocapture`, and `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_accepts_struct_callable_impl -- --exact --nocapture`.
- Recent stdlib/threading and safe TCP verification also used `cargo test -p rock-lib --test integration test_stdlib_thread_spawn_join_supports_unit_result -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_atomic_intrinsics_require -- --nocapture`, `cargo test -p rock-lib --test integration test_tcp_listener_stream_localhost_roundtrip -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdout_write_prefix_uses_safe_handle -- --exact --nocapture`, `cargo run -p rockc -- --entry-file stdlib/lib.rk --crate-name stdlib --no-prelude --emit-artifact build/stdlib.rkca --no-link`, and `cargo run -p rockc -- --entry-file test_projects/new_new/main.rk --extern-artifact stdlib=build/stdlib.rkca --no-link`.
- HKT functional abstraction verification recently used `cargo test -p rock-lib --test integration test_hkt_functor_option_result_vec -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_hkt_applicative_and_monad_laws -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_hkt_foldable_and_traversable -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_traversable_sequence -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_option_result_fp_operators -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_stdlib_vec_map_ref_contextually_types_closure_parameter -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_fresh_stdlib_artifact_invalid_mode_is_deterministic -- --exact --nocapture`, `cargo fmt --all --check`, `git diff --check`, and direct `rockc` runs for `stdlib/lib.rk` plus `test_projects/new_new/main.rk` with an explicit fresh `--extern-artifact stdlib=...` path.
- Fixed-point HKT solver verification recently used serialized focused gates such as `cargo test -p rock-lib infer:: --no-default-features --jobs 1`, `cargo check -p rock-lib --tests`, `cargo test -p rock-lib --test integration test_signatureless_forward_try_and_hkt_map_are_inferred -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_signatureless_listener_accept_callback_try_is_inferred -- --exact --nocapture`, `cargo test -p rock-lib --test integration test_signatureless_hkt_map_result_is_consumed_by_try -- --exact --nocapture`, and a fresh stdlib artifact followed by `target/debug/rockc --entry-file test_projects/new_new/main.rk --extern-artifact stdlib=/tmp/... --no-link`.
