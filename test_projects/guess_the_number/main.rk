// guess the number

> random::random

main = ->
  number = random 1, 100
  tries = 3
  while tries > 0
    guess = input "Guess the number: "
    if guess == number
      print "You guessed it!"
      return
    else if guess < number
      print "Too low!"
    else
      print "Too high!"
    tries -= 1


