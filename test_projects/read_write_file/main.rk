// read write a file

> std::fs::File

main : () -> ()
main = ->
  if Err e = run()
  then print "Error: {e}"
  else print "File written successfully!"

run : () -> Result (), Error
run = ->
  File::open? "test.txt"
   ..write? "Hello, World!"
   ..close!

