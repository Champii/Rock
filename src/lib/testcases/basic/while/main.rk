infix + 4
+ a b = ~IAdd a b

infix < 3
< a b = ~Ilt a b

main =
  let i = 0
  let x = 42
  # This fails because old parser does not handle while keyword
  # Waiting for new parser to be merged
  while i < x
    i = i + 1
  i
