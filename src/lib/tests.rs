


#[test]
fn testcases_if_else_4() {
    use super::Config;

    let input = include_str!("./testcases/if_else/4.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_if_else_3() {
    use super::Config;

    let input = include_str!("./testcases/if_else/3.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_if_else_1() {
    use super::Config;

    let input = include_str!("./testcases/if_else/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_if_else_2() {
    use super::Config;

    let input = include_str!("./testcases/if_else/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_if_else_5() {
    use super::Config;

    let input = include_str!("./testcases/if_else/5.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_for_1() {
    use super::Config;

    let input = include_str!("./testcases/for/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_7() {
    use super::Config;

    let input = include_str!("./testcases/variables/7.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_4() {
    use super::Config;

    let input = include_str!("./testcases/variables/4.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_3() {
    use super::Config;

    let input = include_str!("./testcases/variables/3.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_6() {
    use super::Config;

    let input = include_str!("./testcases/variables/6.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_1() {
    use super::Config;

    let input = include_str!("./testcases/variables/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_2() {
    use super::Config;

    let input = include_str!("./testcases/variables/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_variables_5() {
    use super::Config;

    let input = include_str!("./testcases/variables/5.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_inference_4() {
    use super::Config;

    let input = include_str!("./testcases/inference/4.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_inference_3() {
    use super::Config;

    let input = include_str!("./testcases/inference/3.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_inference_1() {
    use super::Config;

    let input = include_str!("./testcases/inference/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_inference_2() {
    use super::Config;

    let input = include_str!("./testcases/inference/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_inference_5() {
    use super::Config;

    let input = include_str!("./testcases/inference/5.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_functions_4() {
    use super::Config;

    let input = include_str!("./testcases/functions/4.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_functions_3() {
    use super::Config;

    let input = include_str!("./testcases/functions/3.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_functions_1() {
    use super::Config;

    let input = include_str!("./testcases/functions/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_functions_2() {
    use super::Config;

    let input = include_str!("./testcases/functions/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_simple_4() {
    use super::Config;

    let input = include_str!("./testcases/simple/4.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_simple_3() {
    use super::Config;

    let input = include_str!("./testcases/simple/3.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_simple_1() {
    use super::Config;

    let input = include_str!("./testcases/simple/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_simple_2() {
    use super::Config;

    let input = include_str!("./testcases/simple/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_simple_5() {
    use super::Config;

    let input = include_str!("./testcases/simple/5.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_string_1() {
    use super::Config;

    let input = include_str!("./testcases/string/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_string_2() {
    use super::Config;

    let input = include_str!("./testcases/string/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_9() {
    use super::Config;

    let input = include_str!("./testcases/class/9.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_7() {
    use super::Config;

    let input = include_str!("./testcases/class/7.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_4() {
    use super::Config;

    let input = include_str!("./testcases/class/4.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_3() {
    use super::Config;

    let input = include_str!("./testcases/class/3.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_6() {
    use super::Config;

    let input = include_str!("./testcases/class/6.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_1() {
    use super::Config;

    let input = include_str!("./testcases/class/1.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_2() {
    use super::Config;

    let input = include_str!("./testcases/class/2.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_5() {
    use super::Config;

    let input = include_str!("./testcases/class/5.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}

#[test]
fn testcases_class_8() {
    use super::Config;

    let input = include_str!("./testcases/class/8.rk");
    let expected_output = 42;

    let config = Config::default();

    let actual_output = super::run_str(input.to_string(), "main\0".to_string(), config.clone()).unwrap();

    assert_eq!(expected_output, actual_output);
}
